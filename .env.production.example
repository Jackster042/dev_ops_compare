# Production Environment Variables
# Copy this file to .env.production and fill in your actual values
# NEVER commit the actual .env.production file with real credentials!

# Database Configuration (Neon Cloud)
# Use your actual Neon Cloud database connection string
# Format: postgres://user:password@hostname.neon.tech/dbname?sslmode=require
DATABASE_URL=postgres://user:password@your-project.neon.tech/neondb?sslmode=require

# Application Configuration
NODE_ENV=production
PORT=3000

# Arcjet Security (required for production)
ARCJET_KEY=your_production_arcjet_key

# Security Note:
# - Always use SSL/TLS for database connections in production (sslmode=require)
# - Store sensitive values in secure secret management systems (e.g., AWS Secrets Manager, HashiCorp Vault)
# - Never commit actual credentials to version control
# - Use environment variables or secret injection at runtime in production environments
